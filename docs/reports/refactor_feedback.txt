_wsync_pg is not bioreason-specific — it's the generic 2-rank gloo group [0, vllm_dedicated_rank] that any dedicated-rank vLLM weight sync uses. The
   reason the assignment lived inside _setup_bioreason_vllm_rank (pre-slim-down line 2895) is that bioreason was the only caller of dedicated_rank    
  mode at the time, so the PG creation got tucked in alongside the bioreason-specific code (BioReasonModel/ESM3+GO embed load).
                                                                                                                                                      
  What weight_sync.py reads at HEAD splits cleanly:                                                                                                   
  - Generic dedicated-rank state (needs to be restored somewhere): self._wsync_pg, self._total_steps, self._max_generated_tokens, self._temperature,
  self._top_k, self.grpo_samples, plus the matching new_group([0..N-2], "xccl") call on the vLLM rank to keep PG creation order in lockstep with      
  training ranks.                                                                                                                               
  - Bioreason-only state (extracted, not my problem): self._embed_model = BioReasonModel(...), _compute_wsync_layout(self._embed_model).              
                                                                                                                                        
  So Step 1 doesn't restore _setup_bioreason_vllm_rank — it adds a small generic _setup_dedicated_vllm_rank (in vllm_backend.py next to               
  _init_vllm_early_dedicated) that does the PG + gen-param wiring only, then extends it with the new _gen_pg = new_group([0, vllm_rank], "gloo") for
  Phase 2's gen ring buffer. Bioreason can layer back on top later if/when that flow is restored.