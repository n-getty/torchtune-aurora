# Project Status — Aurora RL (torchtune XPU)

Last updated: 2026-08-09 (Kimi-K3 concurrency cap root-caused to odd-tile memory-profiling starvation AND unlocked: KV cache 10,093 -> 117,028 tokens (11.6x), concurrency 4.93x -> 57.1x, correctness holds. See entries below.)

Previous update 2026-07-26 (seq4096 isoFLOPs comparison COMPLETE for both models. Dense Qwen3-4B: eager 3.75% MFU -> flash 8.93% MFU (2.38x). MoE Qwen3-30B-A3B: the mem_reserved-ratchet crash that blocked every attempt is FIXED via `checkpoint_experts` — a new, surgical `torch.utils.checkpoint` wrap around ONLY `self.experts(...)` inside `MoE.forward()` (not the router, not EP dispatch/combine — safe by construction, cannot reintroduce the v158 argsort-tie-break bug). CPU-validated (6/6 new tests + 690/695 full suite), then HW-validated with a 10-step steady-state run: `peak_memory_reserved` plateaus flat at 56.43-56.44 GiB from step 2 through step 10 (zero further ratchet, vs. every prior attempt crashing in that exact range within 1-3 steps) — mechanistic proof the fix works, not a lucky single run. **Final MoE seq4096 number: 265.1 tok/s/gpu steady-state (cov=0.032), ~1.27-1.29% MFU.** Compared to dense's 1562.31 tok/s/gpu / 8.93% MFU at the same seq_len/tile-count/flash-on config: dense is ~5.9x faster tok/s/gpu, ~6.9x higher MFU — a gap that did NOT close at the longer, more realistic sequence length (if anything slightly wider than seq1536's 4.56x), confirming this is real EP/collective/kernel overhead rather than an artifact of the seq1536 alpaca corpus's short/padding-heavy length distribution. The user's standing MoE-vs-dense parity goal remains UNMET at both seq_lens tested. One dumb bug fixed along the way: `_setup_model`'s new `checkpoint_experts` gate initially referenced `cfg`, a name never in scope inside that helper (only `cfg_model` is passed) — fixed by threading `checkpoint_experts: bool` through as an explicit parameter, same pattern as every other `_setup_model` flag. Self-review (AskSage quota exhausted, codex-review unavailable) caught one real gap: `checkpoint_experts=true` was a silent no-op on non-EP configs (the non-EP branch never read it) — fixed with an explicit warning log.)

## Recent changes (newest first)

- **2026-08-09 (cont. 2) — K3 throughput NULL RESULT at short prompts, and the long-context bottleneck relocated to PREFILL.** Two results that close out the throughput plan's measurement phase. **(1) The 11.6x KV unlock did NOT move throughput at the harness's default shape** — c1/c2/c4/c8 = 0.947/1.658/3.484/**6.179** tok/s vs the pre-override best of 6.489 at c=8, i.e. **0.95x, a clean null**. Recording it as such rather than burying it: the cause is the measurement shape, not the KV finding. At a 5-token prompt and 16 output tokens, c=8 needs ~168 token-slots and even the *unfixed* pool held 10,093 — the pool was never the binding constraint, so enlarging it 11.6x could not help. **(2) What the same run DOES establish is the plan's Finding 2, confirmed on hardware: the step is overwhelmingly fixed-cost** — 8x the work costs **1.22x** wall time (16.9s -> 20.7s) and throughput scales **6.52x for 8x concurrency**, near-linear. Concurrency is the right lever. **(3) The decisive long-prompt ladder (`c=4, 1024-in`) then failed to complete one request in ~20 min** on a server answering 5-token prompts in under 30s, and reading `kda.py:212` shows why: **the "vectorized" PREFILL path is `for token_offset in range(max_length)` — it batches across SEQUENCES but still steps one TOKEN at a time**, so a 1024-token prefill is ~70,656 Python-level einsum groups against 69 for a decode step. (The decode path IS genuinely batched, which is exactly why every short-prompt number in this investigation looks sane and every long-prompt attempt stalls.) **Net: prefill is now the gating item for long context.** The KV fix is necessary for long-context concurrency but cannot be *demonstrated* until prefill is tolerable, because the only regime where the pool binds is the regime this loop dominates. Next target is a genuinely token-parallel prefill (chunked / associative-scan delta rule in the torch fallback) — distinct from the descoped Triton KDA kernels, which remain unsafe. Only then is the apples-to-apples 1024+512 comparison against Qwen-480B's 106 tok/s meaningful.

- **2026-08-09 (cont.) — K3 KV starvation CONFIRMED by vLLM's own log and UNLOCKED: 11.6x KV cache, correctness holds.** Re-ran the identical 3-node TP=32/EP-on config with `--diagnostic-blocks 800`, which makes `may_override_num_blocks` print each rank's pre-override `num_blocks` — the direct measurement the `info_once` logging had been hiding. **6 ranks computed 69/76/77 blocks while 26 computed 1331-1724** (9 of them exactly 1709): a ~22x spread across ranks of the same model on the same hardware, with one 69-block rank sizing the pool for all 32. **With the override: GPU KV cache 10,093 -> 117,028 tokens (11.6x), maximum concurrency 4.93x -> 57.14x**, clean startup (HTTP 200, no OOM, no `banned:1`, no engine error) — confirming the memory the override claimed was genuinely present, as the earlier all-tiles-13.8-GiB-free measurement predicted. **Correctness gate PASSED 4/4** on the enlarged pool (`validate_block_override.sh` phase 1): coherent factual greedy output at c=1 and c=2, all logprobs finite. 800 was deliberately conservative — healthy ranks support ~1700, so roughly another 2x of pool remains. **Still a workaround, not the fix.** Preferred workaround is `--kv-cache-memory-bytes`, which skips the broken profiling step entirely rather than overriding its output; the real fix (serialize sibling-tile memory profiling, or treat `available_memory` as a per-card quantity) belongs in the XPU platform/worker layer and is NOT yet written. Note a "clamp negative `non_torch_increase`" patch in shared `mem_utils.py` does not address this — the misattribution is positive-signed (attempted and reverted).

- **2026-08-09 — Kimi-K3 throughput: ROOT-CAUSED the concurrency cap to an odd-tile memory-profiling starvation worth 25x of the KV pool; the plan's "34x KV inflation" hypothesis is REFUTED.** Working the "breaking the K3 throughput ceiling" plan (job 8745911, 3 nodes, TP=32/EP-on, real checkpoint). **(1) Finding 3 refuted for free, on a login node.** The plan blamed `unify_hybrid_kv_cache_specs` for inflating MLA pages ~34x to match a huge KDA state page. Computing the real page sizes from the actual code paths gives 203,520 B/layer/sequence (KDA) vs a 221,184 B unified page, i.e. **8.68% padding** — byte-identical to the `interface.py:630` line every production run already logs. Live `VLLM_LOG_KV_CACHE_SPECS=1` confirms `distinct_page_sizes=1` at stage `initial`, so both unification passes are no-ops. The "~909 KB/token" figure that motivated the hypothesis divided memory by `max_concurrency * max_model_len`, which is a scheduling quantity, not a byte capacity. `--mamba-block-size` cannot help. **(2) The real cause: 11 of 32 ranks — every one an ODD local tile — profile 0.34-0.42 GiB free while 21 ranks report 8.12-8.52 GiB.** `get_kv_cache_configs` clamps every rank to `min_num_blocks`, so one starved rank drags the pool from the 1722 blocks a healthy rank could afford down to **69** — 0.34 GiB allocated of 8.5 GiB available, **25x discarded**. Invisible until now because "Available KV cache memory" is `info_once` (only ~3 of 32 ranks print, and all 3 happened to be healthy) and the K3 block-profile record stores `num_blocks` only AFTER the clamp, so all ranks showed an identical 68 and the distribution looked uniform. The gap is `non_kv_cache_memory` (~8.2 GiB more on odd tiles), not weights — every rank logs the same "Model loading took 47.45 GiB" — consistent with an odd tile's device-wide `mem_get_info` picking up its even sibling's allocation on the shared physical card. This also explains the previously-unexplained 3-attempt memory-fit variance from `K3-step5-reconfirm`. **(3) Step 1's premise is settled: raising `max_num_seqs` 8 -> 32 changed nothing** (`num_blocks` 68 -> 69, concurrency 4.86x -> 4.93x), because the pool is sized before `max_num_seqs` is consulted. **(4) Two benchmark-shape traps found**, both of which make K3 look wedged: `sweep_topology.sh` applies one `PROMPTS` value to every cell, so its 128-prompt default is 128 SEQUENTIAL generations at c=1 (~19h); and the vectorized KDA PREFILL path is gated on `sequence_count >= 4`, so a c=1 256-token prefill silently runs the scalar per-token loop over 69 layers (did not finish one request in 18 min, workers at 120% CPU throughout). **(5) The c>=2 NaN-logprob defect did NOT reproduce** — 4/4 probe cells clean (text coherent, all logprobs finite), so it is not a standing batched-path correctness bug. **Not done: the 25x fix is diagnosed, not applied.** Candidates: serialize sibling-tile memory profiling, treat `available_memory` as per-card, or `--diagnostic-blocks` at the healthy-rank value as an immediate workaround (needs confirming the starved reading is a measurement artifact first), then re-gate correctness and re-measure. Tooling committed: `analyze_block_profile.py`, `run_concurrency_ladder.sh`, `probe_batched_nan_logprobs.py`, `VLLM_LOG_KV_CACHE_SPECS`. See `memory/project_kimi_k3_kv_odd_tile_starvation_rootcause_20260809.md`.

- **2026-08-09 — BioReason `keep_list_target` 2-node smoke PASSED on Aurora (job 8745755).** The Qwen3-32B Stage-1-normalized target ran on `x4112c1s3b0n0` and `x4117c1s4b0n0` for all **15/15** steps with `keep_list_target: true`, XPU fused flash engaged, finite losses (`1.0905 → 1.0821 → 0.9648 → ... → 1.0513`), and no OOM/page-fault/fatal training error. `scripts/check_run_health.sh` returned **GREEN** with 15 distinct SFT step lines; the merged checkpoint and `resume_state.pt` are present under `experiments/bioreason/runs/sft_qwen3_32b_stage1norm_keeplist_smoke/epoch_0`. The first live gate (8745773) failed before model loading because PBS selected an incompatible Python; corrected retry 8745782 explicitly loads `frameworks/2025.3.1` and remains queued. Generation-health, live per-candidate accuracy, and F_max are still **pending**; no claim against echo (`0.6716` weighted) is made yet. A recoverable TorchInductor shared-cache warning occurred during compilation; training continued to completion.

- **2026-08-09 — BioReason GO-pred metric bug FIXED and CONFIRMED with real production data (Phase 0 of the SFT-vs-echo plan, complete on both Aurora and rbdgx3) + binary keep-list target mode ADDED and mirrored (Phase 1, complete on both sides; config/launcher cloned, not launched).** Investigating why no BioReason 32B SFT checkpoint has durably beaten the "echo" baseline (`docs/reports/bioreason_gopred_sft_rerank_target_status_20260809.md`) surfaced a real scorer bug: `score_fmax_scored.py` and `score_fmax_unweighted.py` (both Aurora's `experiments/bioreason/` and rbdgx3's `/raid/ngetty/nemo_rl_work/nemo-rl/` copies) printed `weighted=True` whenever `--ia_file` was supplied but always read cafaeval's UNWEIGHTED best-F dataframe (`best.get("f")`), never the weighted one (`best.get("f_w")`). **Fixed identically on both machines** to read `f_w` when an IA file is given and print both metrics clearly labeled; added an Aurora-side regression test (`test_scorers_read_weighted_f_when_ia_file_given`). **Confirmed with real data, not just an offline probe**: re-scored every existing rbdgx3 prediction directory under the fix (echo, paper-reference SFT/RL, rerank_target step_500/2000/4750, rank_only step_500/3000/4750, four `bothfixes` checkpoints). The rank_only figures previously written into the 2026-08-09 report (0.6888/0.6772/0.6452 "weighted") turn out to be exactly the tool's old unweighted-but-mislabeled output, digit for digit — the real corrected weighted numbers are 0.6321/0.6217/0.5921. **Corrected echo (0.6716 weighted) still beats every re-scored checkpoint** — the best found (`bothfixes_step160`, 0.6492 weighted) trails by 0.0224, a real but much narrower gap than the unweighted framing implied. Full corrected table is in the report's addendum #2. **Implemented the plan's Phase-1 recommendation on both sides**: a new `keep_list_target` mode — candidates-first, bare `GO:XXXXXXX` lines, emitting ONLY in-prompt go_pred candidates also in ground truth (Aurora: `torchtune/dev/bioreason/dataset_sft.py`, reuses `_fit_candidates_first`/`_gt_terms`/`_gopred_terms`/`_rerank_confidence`; rbdgx3: `nemo_rl_bioreason/bioreason_sft_dataset.py` + `sft_32b_stage2_lora_train.py` CLI flag, reusing `_fit_rerank_candidates_first`). Caught and fixed one real port bug along the way: rbdgx3's `_fit_rerank_candidates_first` picks its output header from `self.rerank_rank_only` alone, which would have mislabeled keep_list_target's header as "GO terms (scored):" despite having no scores — fixed to check `(self.rerank_rank_only or self.keep_list_target)`. 13 new CPU tests on Aurora (all passing, full `tests/torchtune/dev/bioreason` suite green except one pre-existing unrelated failure in `test_reward_target_and_propagation.py`); functional smoke test + constructor-guard checks passed directly on rbdgx3 (no CPU test suite there). Cloned (not launched) a config/launcher pair: `sft_bioreason_qwen3_32B_stage1norm_keeplist_xpu.yaml` / `pbs_4n_sft_stage1norm_keeplist.sh`. **Correction to an earlier draft of this entry**: this work was initially reported as blocked on rbdgx3 access; that was an `ssh rbdgx3` (bare hostname, doesn't resolve) vs `ssh rbdgx3.cels.anl.gov` (correct FQDN) mistake on the operator's part, not a real access restriction — rbdgx3 was reachable the whole time. See `~/.claude/plans/create-a-plan-with-twinkly-robin.md` and `docs/reports/bioreason_gopred_sft_rerank_target_status_20260809.md`'s addendum #2 for the full corrected numbers.

- **2026-08-03 — Qwen3.6-27B HF-wrapper integration plan Step 4 PASSED — this is the plan's overall pass criterion, the whole `/home/ngetty/.claude/plans/jiggly-jumping-garden.md` effort's stated goal.** Full 12-tile FSDP2 Stage-2 LoRA smoke (job 8729326, node `x4220c4s7b0n0`, log `experiments/bioreason/logs/step4_stage2_042119/run.log`): real Qwen3.5-27B checkpoint, LoRA (`lora_rank=32, lora_alpha=64`) applied via PEFT directly onto the real HF `Qwen3_5ForCausalLM` (both sub-layer types — attention `q/k/v/o_proj` + MLP `gate/up/down_proj` for full-attention layers, `in_proj_qkv/z/b/a` + `out_proj` for Gated-DeltaNet linear-attention layers), Stage-1-aligned projector loaded and frozen. **5/5 real training steps clean, loss monotonically decreasing `10.7149 → 7.1360 → 6.0735 → 3.5947 → 1.8976`**, ~167-178s/step (fbs=1, seq=6144 — smoke scale only, not a throughput number). `scripts/check_run_health.sh` verdict: **GREEN, RC=0** (run manually after the PBS walltime cap killed the job right after step 5's `resume_state.pt` save, before the launcher's own automated summary block could execute — handled by re-deriving diagnostics from the surviving log, not by discarding or re-running). No `Traceback`/`Error`/`banned`/`OutOfMemory` anywhere in the log. XPU peak memory (smoke scale): active/alloc 8.02 GiB, reserved 9.01 GiB. `autocast_adapter_dtype=False` confirmed present at the `get_peft_model()` call site in `model_native.py` by source inspection (same pattern as the already-HW-validated GRPO LoRA code in `model.py:205-259`). **Four bugs found and fixed en route**: (1) `projector_output_norm` mismatch between Stage-1 (`true`, 4-layer projector `nn.Sequential` with a `LayerNorm` at index 3) and Stage-2's YAML (previously unset, defaulting to `false`/3-layer) — Stage-2's config now sets `projector_output_norm: true` to match Stage-1 exactly, a general footgun for any future two-stage backbone pair; (2) `peft==0.17.1` (`BIOREASON_DEPS`, pinned for other backbones) shadowing `torch211_venv`'s own `peft==0.20.0` (needed for `transformers==5.7.0`/`Qwen3_5*`) via `PYTHONPATH` search order — fixed by prepending the venv's site-packages ahead of `BIOREASON_DEPS` in the Step 4 launcher; (3) PEFT-nesting-prefix handling in the base-checkpoint loader (outer `PeftModel.base_model.model.*` + per-module `.base_layer.*` wrapping) confirmed correct by the run reaching and completing training; (4) PEFT/Conv1d rank-vs-groups incompatibility (Gated-DeltaNet's depthwise conv1d has `groups=10240`, no practical LoRA rank divides evenly) resolved via the plan's documented `include_conv1d_lora: false` fallback, leaving the conv1d frozen. **Explicitly still out of scope per the plan** (this smoke does NOT validate any of these): multi-epoch convergence, throughput/memory tuning at production batch size, vLLM/GRPO rollout integration, weight-sync adaptation for this backbone's LoRA target modules, `torch.compile`/fused Triton RMSNorm-RoPE support, token packing, bucketed-batch-size tuning, full-scale production runs. This result validates mechanics only. See `memory/project_qwen36_27b_step4_stage2_lora_validated_20260803.md` and, for the Step 3 blocker hit one step earlier in this same investigation, `memory/bugs/project_qwen35_hf_rope_meta_device_bug_20260803.md` (HF Qwen3.5's non-persistent RoPE `inv_freq` buffer stays uninitialized under FSDP2 meta-init — fixed via a post-meta-init re-derive branch).

- **2026-08-02 (cont.) — Aurora BioReason 32B tok/s/tile RECOVERED from real production-soak logs: 396.5 tok/s/tile steady-state (job 8674383).** Follow-up to the baseline synthesis below after the user asked for a real tok/s/gpu(tile) number rather than only an MFU%/step-time framing. Parsed the actual `[bioreason-timing] fwd/bwd/optim` per-microbatch lines already logged during the flash+compile+bucketbs production soak run (`experiments/bioreason/logs/prodsoak_174804/run.log`, rank 0) and multiplied by the bucketed sampler's real `bs×seq_len` token count per microbatch — a direct computation from real HW logs, not an estimate. Result: step 1 (one-time `torch.compile`) 178.9 tok/s/tile, step 2 (cache ramp) 214.9, **steady state (steps 3-15, n=13) 396.5 tok/s/tile ≈ 4,758 tok/s/12-tile-node.** This is now the citable Aurora-side number, superseding the earlier "no direct tok/s/tile figure recorded" framing in the 2026-08-02 baseline synthesis entry below. **Face-value comparison against NeMo-RL/H200's ~1,820-2,563 tok/s/GPU: NeMo-RL is ~4.6-6.5x faster per accelerator** — but this ratio is NOT yet a controlled result: Flash Attention/compile engagement remains unconfirmed on the NeMo-RL 32B side (see caveats in the entry below), so if those numbers are pre-flash/pre-compile-equivalent the gap would narrow considerably against Aurora's own pre-flash 101.7s/step number. Updated `docs/reports/nemo_rl_vs_aurora_sft_baseline_20260802.md` with the full per-step table and the caveat that this ratio should not be repeated as settled fact.

- **2026-08-02 — NeMo-RL/H200 vs Aurora/XPU BioReason 32B SFT per-accelerator baseline SYNTHESIZED from existing numbers (no new HW runs this round, per user's explicit choice).** User asked to establish a per-GPU throughput baseline between the NeMo-RL/H200 BioReason port and this repo's production Aurora recipe before deciding where to run further BioReason SFT work. Rather than a fresh matched benchmark, synthesized the best-known numbers already on file into `docs/reports/nemo_rl_vs_aurora_sft_baseline_20260802.md`. **Aurora side** (per-tile, since Max Series GPUs have 2 tiles/GPU — reported in native units, no renormalization): 32B Stage-1 production ladder math-SDPA 101.7s/step -> `TORCHTUNE_USE_XPU_FLASH=1` 51.9s/step (1.96x) -> +compile 70.2s/step at seq6144 (+5.8%) -> +bucketed-bs steady ~78.4s/step (~1.60x combined vs flash-only baseline), MFU ladder 10.0% -> 19.7% -> ~21.7%. **NeMo-RL/H200 side** (per-GPU, no tile split): two 32B Stage-1 runs on file — first 200-step run at ~2,563 tok/s/GPU (3.50s/step, 3 GPUs, data-parallel), full-scale 117k-row run at ~1,820 tok/s/GPU (7.20s/step, 7 GPUs, data-parallel, gradient checkpointing). **Found a real open gap, not papered over**: neither 32B NeMo-RL memory file confirms Flash Attention is actually engaged or that `torch.compile` is applied — the 4B NeMo-RL port's own write-up found an explicit `attention_mask` silently disables Flash in PyTorch's SDPA dispatcher (a major lever there, fwd 1.1s->0.2s), and this exact failure mode is unverified on the 32B Stage-1 path. If unconfirmed, the NeMo-RL 32B numbers may be pre-flash-equivalent, meaning the true H200-vs-Aurora gap at matched attention-fast-path status is currently unknown in either direction. Also flagged: different parallelism (Aurora FSDP2 top-level wrap vs NeMo-RL plain data-parallel replication, since the 32B backbone fits one H200's 143GB budget unsharded) and different dataset/batch composition. **Bottom line: no single defensible ratio exists yet** — recommended next step (deferred, out of scope this round) is a matched-config benchmark with confirmed Flash/compile status on both sides. See the report for full sourcing.

- **2026-08-02 (cont.) — Qwen3.6-27B backbone feasibility spike RESULT: CLEAN on a small-scale replica — the torch-native gated-delta-rule fallback dispatches correctly on XPU with no missing-kernel or illegal-op errors.** Environment check (login node, `torch211_venv`, transformers 5.7.0): `AutoConfig.from_pretrained` on the real `Qwen3.5-27B` checkpoint loads cleanly (`Qwen3_5Config`/`Qwen3_5TextConfig`); confirmed `is_flash_linear_attention_available()=False` and `is_causal_conv1d_available()=False` (the CUDA-only `fla`/`causal_conv1d` packages are absent, as expected on Aurora) — so the code path is HF's own `torch_chunk_gated_delta_rule`/`torch_recurrent_gated_delta_rule` torch-native fallback, confirmed correct (not a custom CUDA kernel requirement). Also found `Qwen3_5ForCausalLM`/`Qwen3_5TextModel` exist as separate exported classes from the vision-wrapped `Qwen3_5ForConditionalGeneration` — a genuine text-only class path exists, better than initially assumed — but the real on-disk checkpoint's safetensors keys use the multimodal `model.language_model.*` prefix (851 language tensors + 333 vision + 15 MTP, 55.6GB total bf16), so loading real trained weights through the text-only class would need a key-remapping step, not tested this round. **HW smoke** (debug-queue hold, node x4307c5s7b0n0): built a SMALL-SCALE random-init replica of the real architecture (same hybrid layer pattern — 3 linear_attention layers per 1 full_attention layer, same head/dim ratios — but 8 layers/hidden_size=512/64.9M params, not the real 64-layer/27B config) and ran 5 forward+backward iterations at bs=2/seq_len=512 in bf16. **Result: CLEAN, no crashes.** Steady-state (iters 1-4, excluding first-call warm-up): fwd 0.068s, bwd 0.161s, ~4470 tok/s at this tiny config, peak XPU memory 1.97GB. **This de-risks the core unknown** (does the linear-attention/gated-delta-rule op set even dispatch on XPU) **but does NOT validate the real 27B model** — this number cannot be extrapolated to 27B scale (65M→27B is a ~415x parameter jump; compute/memory scale with active params, not linearly derivable from this tiny run), no real checkpoint weights were loaded, no vLLM/LoRA/BioReason splice was tested, and the real checkpoint's 55.6GB bf16 backbone alone would need `device_map` sharding across 2+ tiles (same pattern as the existing 32B eval path) to fit alongside activations on a 64GB tile. Script kept at `experiments/bioreason/spike_qwen36_backbone_smoke.py`. **No further integration work done this round per the plan** — LoRA target-module rework (`in_proj_qkv`/`in_proj_z`/`conv1d` vs the hardcoded `q_proj`/`k_proj`/`v_proj`/`o_proj` in `torchtune/dev/bioreason/model.py`), weight-sync adaptation, and vLLM rollout wiring remain a separate, larger decision for the user. See `memory/project_qwen36_27b_feasibility_spike_20260802.md`.

- **2026-08-02 — Qwen3.6-27B backbone feasibility spike STARTED** (user asked to redo BioReason SFT on a more capable/recent backbone than Qwen3-4B/32B, initially named "Qwen3.6-27B"). Investigation found the named model is real (`Qwen/Qwen3.6-27B`, HF, April 2026, "28B params" per safetensors metadata) but is **not** a scaled Qwen3 — it's the Qwen3.5-family hybrid architecture (`model_type: qwen3_5`, `Qwen3_5ForConditionalGeneration`): 16 blocks of (3x Gated-DeltaNet linear-attention -> FFN, 1x full Gated Attention -> FFN), vision tower, MTP heads. The locally available checkpoint at `/lus/flare/projects/ModCon/ngetty/models/Qwen3.5-27B` matches this config exactly and is the closest available proxy (confirmed via HF model card fetch that Qwen3.6-27B's architecture table is identical to Qwen3.5-27B's, with headline diffs being agentic-coding/thinking-preservation training, not architecture). This is a materially different integration than any existing backbone: none of this repo's XPU attention fast paths (`TORCHTUNE_USE_XPU_FLASH`/flex/varlen, all SDPA-only) touch the linear-attention layers; HF's reference fast kernels (`fla`, `causal_conv1d`) are CUDA-only packages, with a plain-torch fallback (`torch_chunk_gated_delta_rule`) that is correct but unmeasured on XPU; `torchtune/dev/bioreason/model.py`'s LoRA/weight-sync code hardcodes attention module names (`q_proj/k_proj/v_proj/o_proj`) absent on the linear-attention layers (`in_proj_qkv`, `in_proj_z`, `conv1d`, ...). transformers 5.2.0+ is required for `qwen3_5` support (landed in v5.2.0, Feb 2026) — the `frameworks` module pins 4.57.6 (too old), the separate `torch211_venv` has 5.7.0 (sufficient). vLLM has a native `vllm/model_executor/models/qwen3_5.py` upstream (promising for rollout; XPU kernel status unverified). User chose a small time-boxed feasibility spike (login-node env check, then a single-tile HW smoke of the text-only backbone via the torch-native fallback path) before committing to full BioReason integration. Spike in progress — see follow-up entries for result.

- **2026-07-26 — Dedicated report written consolidating the full MoE-vs-dense MFU parity investigation.** `docs/reports/moe_vs_dense_mfu_parity_20260726.md` — what's been measured (seq1536 alpaca: dense 907.39 vs MoE 199.18/~203 tok/s/gpu, 4.56x gap; seq4096 synthetic: dense 1562.31 vs MoE 265.1 tok/s/gpu, ~5.9x gap), everything tried (EP all2all dispatch, sequential-vs-padded-BMM experts, grad-release batching+transport, the mem_reserved ratchet's two occurrences and root cause, activation offloading tried-and-abandoned, `checkpoint_experts` landed), and what's still blocked (the core parity goal is unmet at both seq_lens on two different datasets — real EP/collective overhead, not a corpus artifact; EP dispatch+combine, 61% of forward time at seq4096, is the clearest unattempted next lever). Synthesizes ~4 days and ~3,300 lines across the two working memory files into one reference doc so future sessions don't re-derive closed conclusions (padded-BMM permanently unviable, grad-release transport already optimal, activation offloading has a real unfixed XPU bug) or re-litigate the corrected mem_reserved-ratchet diagnosis.

- **2026-07-26 — MoE seq4096 mem_reserved-ratchet crash FIXED via `checkpoint_experts`; seq4096 isoFLOPs comparison now COMPLETE for both models.** Direct follow-up to the confirmed-but-unfixed diagnosis below: the crash is a genuine forward-activation `mem_reserved` ceiling (XPU can never `empty_cache()`, so freed activation memory becomes stranded `reserved` that only ever grows within a run — microbatch 0's own fwd+bwd already consumes ~57GB of 64GB, leaving no room for microbatch 1's forward). Tried `enable_activation_offloading=true` first (the originally-named fix candidate) and ABANDONED it: two independent HW attempts crashed with `RuntimeError: NULL pointer argument in memory copy operation` on two DIFFERENT tensors (the router's fancy-indexed `top_scores`, then a `.scatter()` result in `_token_combine_all2all`) even after a codex-review-recommended `.clone()` fix for the first one, each followed by a reproducible multi-minute node-SSH-unresponsiveness episode — concluded (with codex-review concurrence) this is a genuine, previously-undiscovered XPU-backend bug in torchtune's `OffloadActivations`, out of scope to fully fix here (worth filing as a standalone issue separately). **Pivoted to a new, more surgical mechanism: `checkpoint_experts`** — `torch.utils.checkpoint.checkpoint()` wrapping ONLY the `self.experts(routed_input, num_tokens_per_expert)` call inside `MoE.forward()` (`torchtune/modules/moe/moe.py`), explicitly NOT the router or `_ep_dispatch`/`_ep_combine`. This is safe in a way whole-block MoE AC is not: `self.experts()` is a deterministic function of already-fixed inputs computed once by the router/dispatch before this call, so recompute during backward touches no EP dispatch/combine state and cannot reintroduce the v158 argsort-tie-break correctness bug that `_apply_split_ac` exists to avoid. New pieces: `MoE.__init__` gained `checkpoint_experts: bool = False`; `torchtune/dev/rl/distributed.py::_apply_expert_checkpointing(model)` toggles it on every `MoE` submodule post-construction (mirrors `_apply_split_ac`'s convention); the recipe gates it via a new `checkpoint_experts` config key. **CPU-validated first**: 6 new tests (`tests/torchtune/dev/full_finetune_moe/test_checkpoint_experts_equivalence.py`) proving bit-exact output/grad equivalence (both padded-BMM and `SEQUENTIAL_EXPERTS` paths), that EP dispatch/combine fire exactly once (not re-invoked by the recompute), and that the mechanism genuinely engages `torch.utils.checkpoint` rather than being a silent no-op; 690/695 full suite (5 pre-existing unrelated failures). **First HW attempt hit a dumb wiring bug** (`_setup_model`'s new gate read `cfg`, never in scope inside that helper — only `cfg_model` is a parameter) — fixed by threading `checkpoint_experts: bool` through explicitly, same pattern as every other `_setup_model` flag. **Second attempt (after confirming the node had recovered from the earlier offloading-induced instability) — SUCCESS**: 3/3 clean steps, `Training complete`, checkpoint saved, `peak_memory_reserved` plateaus at ~56.5GB by step 2 instead of continuing to climb. **Extended 10-step run on a fresh node (mechanistic confirmation, not a fluke)**: 10/10 clean steps, `peak_memory_reserved` flat at 56.43-56.44 GiB from step 2 through step 10 — zero further ratchet across 9 full steps, vs. every prior attempt crashing in that exact range within 1-3 steps. **Final steady-state number (steps 2-10, step 1 excluded as warmup): 265.1 tok/s/gpu (cov=0.032), ~1.27-1.29% MFU.** Compared to dense-4B's seq4096 flash number (1562.31 tok/s/gpu, 8.93% MFU): **dense is ~5.9x faster tok/s/gpu, ~6.9x higher MFU** — a gap that did NOT close at the longer, more realistic sequence length (slightly wider in tok/s/gpu terms than seq1536's 4.56x), confirming — now at a second seq_len and a second (synthetic, not corpus-length-confounded) dataset — that this is real EP/collective/kernel overhead, not an artifact of any one corpus. **The user's standing MoE-vs-dense parity goal remains unmet.** `checkpoint_experts` unblocked getting a number at all; it is a memory-for-recompute-time trade, not a throughput optimization, so it was never expected to close the gap by itself. The most promising unexplored levers remain EP dispatch/combine (17% of step time in the seq1536 sub-phase breakdown) and expert-compute kernels (10.3%) — not yet re-profiled at seq4096. See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` (`checkpoint_experts FIX` section) for the complete diagnostic trail including the abandoned activation-offloading attempts.

- **2026-07-25 (cont.) — seq4096 realistic-length isoFLOPs MoE-vs-dense comparison using SYNTHETIC fixed-length data: dense side COMPLETE (flash 2.38x win), MoE side genuinely BLOCKED by a new grad-release ceiling.** Direct follow-up to the MFU-20% push (below): user asked to re-measure at a longer, more realistic sequence length with synthetic data (decouples the isoFLOPs comparison from any real corpus's length distribution; standard framework MFU-reporting practice), explicitly because the seq1536 alpaca comparison's short/highly-variable length structurally blocked flash for BOTH models. **Built `torchtune/datasets/_synthetic.py`** (`SyntheticFixedLengthDataset`/`synthetic_fixed_length_dataset`) — every sample exactly `seq_len` tokens, so the standard non-packed SFT collate is a true no-op on padding and never constructs a `"mask"` key, letting `mask=None` reach attention with zero new masking code. CPU-proven with 23 new tests (`tests/torchtune/datasets/test_synthetic_fixed_length_dataset.py`) including a direct pin of the collate no-op/no-mask-key claim. **Caught and fixed one real bug on first HW attempt** (`Qwen3Tokenizer` has no `vocab_size` attribute — only `encoder`, a dict): added an `_infer_vocab_size()` fallback chain, CPU-tested against a tokenizer shaped exactly like the real one, plus explicit `vocab_size: 151936` in both new configs as defense-in-depth. New sibling configs (seq1536 alpaca configs UNCHANGED, remain the permanent short-sequence data point): `qwen3_4b_sft_seq4096_synthetic_xpu.yaml`, `qwen3_30b_a3b_sft_ep8_seq4096_synthetic_xpu.yaml`. Made both benchmark launchers' `CONFIG` env-overridable (previously hardcoded) with zero other logic changes. **Dense Qwen3-4B @ seq4096 (NPROC=8, matching MoE's tile count, batch_size=2, both legs GREEN + `check_run_health.sh --compare` A/B-parity-confirmed): eager (math-SDPA) 12.483s/step, 656.27 tok/s/gpu, 3.75% MFU -> `TORCHTUNE_USE_XPU_FLASH=1` 5.244s/step, 1562.31 tok/s/gpu, 8.93% MFU — a real 2.38x speedup**, `xpu_flash=engaged` confirmed on all 8 ranks, loss trajectories sane both legs. First validation of flash on this project's own dense-4B MoE-comparison model/config (previously only validated on BioReason 32B / Qwen3-8B). **MoE Qwen3-30B-A3B @ seq4096: eager/math-SDPA cannot run AT ALL on a 64GB tile** — OOM'd at `batch_size=2` (step 1 backward) AND at `batch_size=1` (step 2 backward, the documented fallback), both non-device-corrupting (confirmed `torch.xpu.device_count()` stayed at 12 after each). This is itself a real finding: at seq4096, flash is a HARD REQUIREMENT for MoE to fit at all, not merely a throughput lever. **Flash (`batch_size=1`) unblocks the forward/backward OOM — `xpu_flash=engaged` confirmed, step 1 completes with a real loss value — but exposes a DIFFERENT, new ceiling**: crashes entering `_ep_release_fsdp_unsharded_grads`'s `torch._utils._flatten_dense_tensors` call with `UR_RESULT_ERROR_OUT_OF_RESOURCES` (default `TORCHTUNE_EP_GRAD_RELEASE_CHUNK_GROUPS=8`), or with a `banned:1` GPU page fault at `CHUNK_GROUPS=2` (step 3) and again at `CHUNK_GROUPS=1` + `compile=false` (step 1, ruling out BOTH chunk size and compile-recompilation-churn as the cause in the same diagnostic run). **Conclusion: this is a genuine per-param unsharded-gradient-tensor SIZE ceiling** (not a batching/chunk-count issue — every tested chunk size still has to flatten at least one individual param's full unsharded grad) — the same STRUCTURAL class of problem as the seq1536 padded-BMM ceiling (a tensor exceeding the tile at this token volume), just surfacing in the backward/grad-release path instead of the forward padded-BMM allocation, because flash freed enough forward-activation memory to get past the earlier OOM only to expose this different downstream limit. **No cheap fix available within this session's scope** — `batch_size` already at floor, `gradient_accumulation_steps` already proven not to help once `batch_size=1` (2026-07-25 earlier finding), grad-release chunk size just proven irrelevant here. A real fix would need genuinely new engineering: per-param (not per-chunk) STREAMING release in `_ep_release_fsdp_unsharded_grads` (reduce one param's unsharded grad, release it immediately, move to the next — never holding more than one param's flat buffer at a time). **Per every ⁠`banned:1` incident, both corrupted nodes were IMMEDIATELY abandoned and their PBS holds `qdel`'d** — no reuse attempted. **Result reported honestly per `docs/RESULTS_DISCIPLINE.md`: no MoE seq4096 throughput/MFU number exists and none is estimated.** New follow-up task tracked for a future session with a larger HW time budget. See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` (Phase 4, two entries) for the complete diagnostic trail across all 5 HW attempts (dense eager, dense flash, MoE eager bs2, MoE eager bs1, MoE flash bs1 at 3 different chunk/compile settings).

- **2026-07-25 (cont.) — MFU-20% push: landed torch.compile (+2.05%, new default) and definitively CLOSED the mem_reserved ratchet investigation (padded-BMM confirmed permanently unsafe, not just currently-blocked); also landed 5 external-review fixes.** User directive: match the BioReason 32B dense SFT flash+compile win (~20-22% MFU) on Qwen3-30B-A3B MoE SFT, explicitly staying on the alpaca corpus (declined switching to a long-context corpus, which would have broken the existing matched dense-vs-MoE comparison and structurally favored flash). **Phase 0 — external review fixes, all CPU-verified (657 passed, only 5 pre-existing unrelated peft-version failures):** (1) guarded `optimizer_in_bwd=True` + `expert_parallel_degree>1` (EP defers grad sync post-backward; optimizer_in_bwd steps during backward on unsynced grads — silent corruption risk, not previously triggered by any config but now hard-guarded); (2) fixed a tautological gradient comparison in `test_ep1_no_ep_equivalence.py` (was comparing `experts.gate_proj.grad` to itself across two backward passes through the SAME module — now uses independent snapshots, verified the two paths genuinely agree); (3) fixed stale docs (`expert_parallelism.md`'s WS10 sender said "not implemented" — it is; `moe_integration.md`'s "36 transformer layers, 48 MoE layers" was internally impossible against `num_layers=48`); (4) hardened (not fixed, since unreachable on the current architecture) the `_ep_release_fsdp_unsharded_grads` asymmetric-None-skip pattern flagged as a v65-bug-class risk, with a loud log if it ever fires. **Phase 1 — new ground-truth sub-phase timing** (`torchtune/modules/moe/_step_timing.py`, `TORCHTUNE_MOE_STEP_TIMING=1`, XPU-synchronized forward/backward brackets, CPU-validated before any HW time): steady-state breakdown of the 14.57s/step baseline — grad-release 48.5% (unchanged, confirms the prior py-spy 52.1% via an independent method), attention 18.0%, EP dispatch+combine 17.0%, expert compute (SEQUENTIAL_EXPERTS Python loop) 10.3%, router 0.6%, optimizer 2.0%. **Phase 2a — torch.compile A/B, LANDED**: 14.571s/step eager vs 14.272s/step compiled (~2.05% faster), `check_run_health.sh --compare` GREEN parity, loss trajectories within 0.03-0.05% relative difference (bf16 fusion drift, not divergence). Unlike the earlier plain-dense-4B result (compile was a small loss there), this context has more compile-able surface (attention+router+EP-glue at ~35.6% combined) around `GroupedExpertsHF`'s `@torch.compiler.disable` graph break — flipped `compile: false -> true` in `qwen3_30b_a3b_sft_ep8_xpu.yaml`. **Phase 2b — mem_reserved ratchet investigation, CLOSED with a definitive negative.** Added `TORCHTUNE_MOE_MEM_RATCHET_DEBUG=1` (`torch.xpu.memory_stats()` bracketing around grad-release and the optimizer step) and re-ran the exact `gradient_accumulation_steps=2` padded-BMM config that previously crashed. Found: microbatch 0's grad-release call itself genuinely grows `reserved` by +2.6GB (56.3->59.0GB) — a real, attributable transient — but the run then **crashed during microbatch 1's OWN `.backward()`, before microbatch 1 ever reached its own grad-release checkpoint.** This proves the crash is payload-driven (each microbatch's own padded-BMM activation peak stacking on an ever-rising `reserved` floor XPU can never reclaim), not an avoidable allocator artifact from grad-release — even a hypothetical zero-cost grad-release wouldn't have prevented it. **This closes padded-BMM as a lever for this recipe/corpus/scale permanently** (`SEQUENTIAL_EXPERTS=1` is now confirmed the correct PERMANENT default, not a placeholder), superseding the "remaining options" framing in the entries below. **Net result of the full MFU-20% push: 199.18 -> ~203 tok/s/gpu (~2% improvement via compile), MFU still ~1%.** The 20% target was not reached and is very unlikely reachable on this corpus without either a from-scratch padding-free/grouped Triton expert kernel (would attack the 10.3% expert-compute slice without padded-BMM's memory ceiling — not attempted, correctly deferred pending this exact profiling data per the original plan's sequencing) or accepting a different corpus regime where flash's O(S²) savings apply (declined this session to preserve the existing matched dense-vs-MoE comparison). See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` for the full session trail including the `torchtune/modules/moe/_step_timing.py` methodology, all raw timing tables, and the `TORCHTUNE_MOE_MEM_RATCHET_DEBUG=1` snapshots.

- **2026-07-25 (cont.) — HW-tested the `gradient_accumulation_steps` fix for the padded-BMM crash: mechanism confirmed working but insufficient; `gradient_accumulation_steps=4` REFUTED as a further lever.** Direct continuation of the padded-BMM root-cause investigation (below), pursuing the CPU-correctness-proven zero-new-code fix hypothesis (splitting `batch_size=2/gradient_accumulation_steps=1` into `batch_size=1/gradient_accumulation_steps=2`, proven bit-identical to unchunked gradients via 7 new CPU tests). **Ran the real recipe with all 5 required non-default overrides verified in the config dump** (`TORCHTUNE_EP_ALL2ALL=1`, `TORCHTUNE_MOE_SEQUENTIAL_EXPERTS=0`, `max_seq_len=1536`, `batch_size=1`, `gradient_accumulation_steps=2`). **Result: the chunking mechanism itself works exactly as designed** — per-call `total`/`max_count` dropped ~2× vs the `batch_size=2` baseline (confirming EP dispatch correctly halved), and `mem_alloc` correctly reset from 51.27GB (end of microbatch 1's 48-layer forward) to 19.60GB (start of microbatch 2) — proving microbatch 1's `.backward()` genuinely freed its activation graph. The run got 2× further than the `ga=1` crash (96 padded-BMM calls, a FULL 2 microbatches, vs dying at call ~29-34 before). **But it still crashed** (`UR_RESULT_ERROR_OUT_OF_RESOURCES`, rank3, during backward, ~66GB) because `mem_reserved` (the number that actually gates new allocations) never dropped between microbatches — XPU can never `empty_cache()` (the standing FSDP+XPU UR-handle-leak constraint), so freed *allocated* memory becomes cached/*reserved* rather than returned, and `mem_reserved` **ratchets up monotonically across the whole run regardless of grad-accumulation chunking**. **Tried the obvious next experiment, `gradient_accumulation_steps=4`, expecting more headroom — REFUTED.** Reused the same node (still clean after the `ga=2` crash) and ran the identical config with `ga=4`: it crashed at the **exact same point** (96 calls) with **numerically indistinguishable** `total`/`max_count` values to the `ga=2` run (differences only in the 4th-5th significant figure — routing-noise, not a real reduction). **Root cause of why this failed**: `gradient_accumulation_steps` does not further subdivide an individual microbatch — it only controls how many `batch_size`-sized microbatches accumulate before the optimizer step. `batch_size` was already at its floor of `1` (the packed dataset's smallest unit), so there was no further per-microbatch token reduction available on this axis; increasing `ga` beyond 2 changes optimizer-step cadence, not the crash-causing per-layer peak. **The `gradient_accumulation_steps` axis is now closed** — `ga=2` is the best point on it, and it's confirmed insufficient. Remaining options for a future session: reduce `tokenizer.max_seq_len` (the only lever left below `batch_size=1`, though it changes the token-volume-per-step invariant this whole comparison was built on), investigate the `mem_reserved` ratchet mechanism directly (a suspicious 5.9GB jump happened exactly at the microbatch-1→microbatch-2 boundary, not during either microbatch's own layer loop — worth `torch.xpu.memory_stats()` bracketing to see if it's avoidable), or accept `SEQUENTIAL_EXPERTS=1` as the correct working default and redirect further throughput effort to the still-open 29.2% "unattributed backward compute" py-spy bucket from the earlier re-profile. Updated the YAML config comment and memory with the full corrected trail (including that the ga=4 "obvious next step" recommendation from the earlier writeup was itself wrong). See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` for the complete session trail.


- **2026-07-25 (cont.) — Padded-BMM crash ROOT-CAUSED via HW debug instrumentation + fix concept CONFIRMED in a standalone repro; NOT an L0 handle-table bug.** Direct continuation past the prior entry's "padded-BMM CRASHES, closed as a lever" conclusion, per explicit user instruction to isolate and fix rather than stop at documentation. **Four synthetic single-process isolation repros (plain padded-BMM loop, 8-concurrent-process simulating multi-rank L0 contention, ~48GB memory-pressure preload, FSDP2-solo-wrapped) all ran CLEAN** at an estimated ~3,072-tokens/rank shape — none reproduced the crash. The tell: all four called `.backward()` after every simulated layer, freeing that layer's activation graph immediately — not what the real recipe does. **Ground truth captured via temporary env-gated instrumentation** (`TORCHTUNE_MOE_BMM_DEBUG=1`, added to `GroupedExpertsHF.forward()`, safe/off-by-default, left in the code): re-ran the real recipe and captured the actual per-layer shapes right up to the crash. Two findings, both real: (1) **real per-layer token count averages ~27,479 — the original estimate (~3,072) was wrong by ~9×** (a mis-derived post-EP-dispatch average); (2) **per-expert routing is severely imbalanced** — `max_count` (the padded tensor's per-expert dimension) averages ~8,041, i.e. **~4.6× more padding overhead than an even split would need**. `mem_reserved` climbed monotonically and never dropped — 11.92GB → 66.79GB across 34 of 48 captured layers — because **MoE layers are deliberately excluded from activation checkpointing** (the v158 router-determinism correctness fix) and this un-chunked SFT `batch_size=2` path has no `forward_batch_size`-style chunking, so every layer's padded-BMM activations must stay resident simultaneously until the single end-of-forward `.backward()`. **This is a genuine activation-memory ceiling, not the L0 handle-table/allocation-count bug** the original GRPO-context `fbs>=4` fix addressed — same `UR_RESULT_ERROR_OUT_OF_RESOURCES` symptom, different mechanism. **Confirmed standalone**: a corrected repro (`recipes/dev/repro_padded_bmm_v2.py`, single forward across 48 layers at the real observed shape distribution, THEN one backward) reproduces the crash with zero FSDP/EP/distributed machinery — crashed at layer 29/48, 66.20GB allocated/67.97GB reserved, matching the real HW run's qualitative pattern closely. **Fix concept validated in the same fast harness**: chunking the 48-layer forward into groups with a backward per chunk (mirroring GRPO's existing `forward_batch_size`/`CHUNKED_BACKWARD` mechanism) stays clean at every tested chunk size — peak `mem_reserved` 26.9GB (chunk=4), 35.4GB (chunk=8), 46.6GB (chunk=16), all comfortably under the 64GB tile vs the unchunked path's ~68GB crash. **Padded-BMM is therefore not a dead end** — its real, previously-measured 6.3× speedup over `SEQUENTIAL_EXPERTS=1` is recoverable once forward_batch_size-style chunking is ported into `full_finetune_moe_distributed_xpu.py`'s train loop (not done this session; this session validated the fix concept in an isolated repro, not the production implementation). Updated the YAML comment (`qwen3_30b_a3b_sft_ep8_xpu.yaml`) to reflect the confirmed root cause and fix path rather than the earlier "closed, unfixable" framing. See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` for the full trail (debug instrumentation output, all four repro scripts, chunk-size sweep data).

- **2026-07-25 — Post-grad-release MoE EP=8 re-profile: dispatch/combine permutation-loop hypothesis REFUTED; padded-BMM CRASHES at batch_size=2, correcting a wrong config assumption.** Direct continuation of the grad-release investigation (below) after explicit user pushback on stopping once a bottleneck is merely documented rather than pursued toward a fix. With grad-release closed (bandwidth-bound, ~52% of step, XCCL already optimal), formed and tested a concrete hypothesis for the remaining ~48%: that `_token_dispatch_all2all`/`_token_combine_all2all` (`torchtune/modules/moe/_parallelism.py:929-1103`) spend meaningful time in nested Python loops with per-element `.item()` calls (each an XPU device sync — up to 8×16×8=1024 calls/layer × 48 layers). **Took 4 HW attempts to get a valid profile — 3 failed on self-inflicted launcher-invocation mistakes, not real bugs**: (1) omitted `TORCHTUNE_EP_ALL2ALL=1` (launcher's own code default is `0`/AG+RS, not `1`) → OOM on the wrong dispatch path; (2) relaunched correctly but reused a node that had just OOM'd → hit a `banned:1` page fault (L0 device-state corruption, abandoned per standing rule); (3) on a fresh node, omitted `EXTRA_OVERRIDES='tokenizer.max_seq_len=1536 batch_size=2'` (config's own default is `max_seq_len=2048`) → silently ran a 33%-more-tokens/~1.78×-more-compute config → anomalous 78s step-1 (vs ~20s expected) → `UR_RESULT_ERROR_OUT_OF_RESOURCES`. **Attempt 4, both required non-default overrides verified present before proceeding**: ran clean 40/40 steps, GREEN, median 14.16s/step (matches established baseline). **py-spy result (48 samples, 8 ranks × 2 rounds): 52.1% grad-release (already closed), 29.2% unattributed real backward compute (native C++ autograd kernel execution py-spy can't resolve further — needs Kineto/manual `torch.xpu.synchronize()`-bracketed timing instead), 4.2% MoE router forward, and only ~4% in dispatch/combine — ALL of which was inside the actual `all_to_all_single` collective call itself, ZERO samples in the Python permutation-building loops.** Hypothesis REFUTED — the loops operate on small (`ep_degree=8`/`num_local_experts=16`-sized) int lists and are apparently cheap enough not to register. Also confirmed `_xpu_all_to_all_via_gloo` (a misleadingly-named function) genuinely routes through native XCCL, not gloo, per its own docstring ("v80 fix: remove gloo path entirely"). **Follow-up test of the next flagged lever — padded-BMM at batch_size=2 — CRASHES immediately** (`UR_RESULT_ERROR_OUT_OF_RESOURCES` at `_experts.py:123`, step 0, zero steps completed), directly refuting the config's own comment that `batch_size=2` is "below the fbs>=4 crash threshold" (that number was validated in a GRPO/RL context and does not transfer to this SFT recipe/config). `TORCHTUNE_MOE_SEQUENTIAL_EXPERTS=1` is therefore load-bearing here, not a conservative safety margin — corrected the YAML comment (`qwen3_30b_a3b_sft_ep8_xpu.yaml`) to reflect this. **Net: both flagged lever candidates for the remaining ~48% (dispatch permutation-vectorization, padded-BMM) are now closed — one refuted as unnecessary, one refuted as unsafe.** The 29.2% unattributed-backward-compute bucket remains the only open avenue, but requires a different profiling method than py-spy to break down further. **Standing lesson captured** (`memory/feedback_launcher_config_drift.md`): this "best-known-good" MoE SFT config requires BOTH `TORCHTUNE_EP_ALL2ALL=1` and `EXTRA_OVERRIDES='tokenizer.max_seq_len=1536 batch_size=2'` as explicit, non-default overrides — never assume a generic launcher invocation reproduces a validated result; re-derive every override from a prior known-good log each time. See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` for the full session trail (includes all py-spy dumps and crash tracebacks).

- **2026-07-24 (cont.) — Phase 1c CLOSED: matched dense-4B-vs-MoE-30B-A3B SFT throughput comparison, replacing an estimate with a real measurement — dense is 4.56× faster tok/s/gpu at truly matched config.** The MoE SFT investigation's "~4.4x off dense-per-tile" note (see the grad-release entry below) was derived from the seq2048 dense-4B reference number (9,934 t/s/12 tiles, a *different* seq_len than the MoE run's seq1536) — not a fair comparison. Ran a genuinely matched baseline: same recipe (`full_finetune_distributed_xpu.py`, `torchrun --standalone`), same 8-of-12-tile topology, same `batch_size=2`/`tokenizer.max_seq_len=1536` as the MoE EP=8 run, via `benchmarks/sft_throughput_aurora_vs_polaris/run_aurora_torchtune.sh NPROC=8` on a fresh debug-queue hold (job 8699055). **The run appeared stalled ~11 min at checkpoint load** — two `py-spy` snapshots confirmed genuine (if slow) DAOS/Lustre I/O progress inside `load_from_full_model_state_dict` → a post-load `barrier()`, not a hang; training itself then ran fast and clean (12/12 steps, loss monotonic 2.22→1.22, `rc=0`). **Result** (`check_run_health.sh` GREEN, 10 measured steps after 2-step warmup): **907.39 tok/s/gpu**, step_time_median=3.175s, tight spread (`cov=0.056`). **Matched comparison: dense 907.39 tok/s/gpu vs MoE's 199.18 tok/s/gpu (all2all/packed/no-flash, same seq_len/batch_size/tile-count) = dense is 4.56× faster.** Notable because Qwen3-30B-A3B activates ~3.3B params/token — *fewer* than dense Qwen3-4B's 4B — so a compute-efficient MoE would be expected to match or beat dense tok/s/gpu at equal-or-fewer active FLOPs, not trail it 4.56×; this confirms the gap is real EP/collective/kernel overhead, not an inherent MoE-vs-dense FLOP disadvantage. Per the grad-release investigation below, ~48% of MoE step time is one specific bandwidth-bound function already proven un-improvable — the next lever for closing this gap is EP dispatch/combine or expert-compute kernels, not grad-release. **This closes task #28 (Phase 1c).** See `memory/project_moe_sft_ep1_loss_climb_investigation_20260723.md` (final section) for the full writeup.

- **2026-07-24 — Qwen3-30B-A3B MoE SFT EP=8: `_ep_release_fsdp_unsharded_grads` grad-release allreduce batched (v166→v168), HW-validated correct, then proven to be a real bandwidth floor (not a bug) via per-component timing + an XCCL-vs-gloo A/B — this specific function is now a CLOSED optimization avenue.** Follow-on to the 2026-07-23 profiling session's finding that ~60% of py-spy samples during Qwen3-30B-A3B EP=8 SFT steady-state land in this one function's `all_reduce` call (the function fires one small collective per FSDPParamGroup — ~97 separate allreduces/step at this scale, since EP forces FSDP2's own batched `reduce_grads` off). **Fix (v166→v168):** rewrote the function to bucket all FSDPParamGroups sharing a `(PG, dtype)` into one flattened `_flatten_dense_tensors`/`all_reduce`/`_unflatten_dense_tensors` call per chunk (`TORCHTUNE_EP_GRAD_RELEASE_CHUNK_GROUPS`, default 8 groups/chunk — bounds peak transient memory; a single global batch (v166) HW-crashed with `UR_RESULT_ERROR_OUT_OF_RESOURCES` at step 2). **v167 HW crash (`banned:1` at step 9) root-caused and fixed in v168**: `_unflatten_dense_tensors` returns VIEWS into the shared flat buffer, not copies — those views could reach `sharded_param.grad` and leak the whole chunk's buffer per param across steps; fixed with an explicit `.clone()` after unflatten. **Correctness: CPU equivalence test** (`tests/torchtune/dev/rl/test_ep_grad_release_batching_equivalence.py`, gloo/multi-rank, mirrors the existing `test_ep_all2all_dispatch_equivalence.py` pattern) proves bit-exact output vs a hand-reproduced unbatched reference, both `accumulate_into_grad=False/True`, AND that the collective count actually drops (proving the batching isn't a no-op). **HW loss/grad_norm parity** (10 steps, same seed, pre- vs post-fix): max relative loss difference 0.02% across all steps — numerically transparent to training. **The throughput result was surprising and required two follow-up investigations to explain correctly.** Naive HW A/B showed only a ~1.5% win (202.07 vs 199.18 tok/s/gpu) despite py-spy's ~60% share — an independent codex-review pass (model `asksage-gpt-5.6-luna`) confirmed the fix itself is sound but flagged that py-spy's statistical sampling cannot distinguish removable collective-*launch* overhead (what batching fixes) from real communication time or cross-rank sync wait (what it doesn't) — and separately caught that my own write-up had over-stated the collective-count reduction (97→~13 at default chunk=8, not 97→1). **New per-component XPU-synchronized timing** (`TORCHTUNE_EP_GRAD_RELEASE_TIMING=1`, brackets each phase with `torch.xpu.synchronize()` so timings reflect real GPU completion, not async-launch return) then gave a **definitive** answer: **99.2% of the function's wall-clock time is genuinely inside the `all_reduce` call itself** (barrier/host-prep/pass3 combined <1%). A follow-up chunk-size A/B (8→32 groups/chunk, cutting collectives 13→4, a 3.25× reduction) changed `all_reduce` time by only **+0.6%** — proof the cost is bandwidth-bound, not launch-overhead-bound, closing the chunk-tuning axis. **Final XCCL-vs-gloo A/B** (forcing `TORCHTUNE_EP_GRAD_RELEASE_XCCL=0`, same config): gloo is **2.56× slower on the raw all_reduce**, **3.36× slower on the whole function** (its `.cpu()` round-trip inflates collect+flatten 407× and the post-collective pass3 90×), and **2.77× slower end-to-end per step** (39.57s vs 14.30s, both `check_run_health.sh` GREEN on the DiskLogger metrics log) — confirming the launcher's existing `TORCHTUNE_EP_GRAD_RELEASE_XCCL=1` default is already optimal and there is no further transport-level lever here. **Net verdict**: the ~7s/microbatch this collective costs (~48% of the ~14.3s XCCL step) is a genuine bandwidth floor for this payload/world-size, not a bug or a missed optimization — v168's correctness fix stands and should stay, but further throughput work should target EP dispatch/combine or expert compute, not this function. See `memory/project_moe_sft_profiling_gradrelease_bottleneck_20260724.md` for the full session-by-session trail (includes the codex-review transcript and all raw timing tables).

- **2026-07-15 (cont.) — Flash-native MFU levers after packing was refuted: AC-off OOMs (activation ceiling), bucketed-bs on flash VALIDATED ~1.49× throughput.** With packing dead (flex too slow) the remaining question was: what raises MFU using flash's *own* strengths (plain-causal, `mask=None` — no flex), spending the ~43 GiB/tile flash freed? **First, a config clarification (the user asked "are we LoRA or full-FT?"): NEITHER** — stage1norm is frozen-backbone + trainable-projector-only (~72M params; `enable_lora=false`, `freeze_backbone=true`). The ~21 GiB/tile flashON floor is base weights (~5 GiB sharded) + activations (~16 GiB), with ~zero backbone grad/optimizer state. **This means LoRA cannot help the AC-off/bs>1 memory problem — those are activation-bound, and activations are identical for frozen/LoRA/full-FT (you backprop through all 64 layers to reach the projector regardless); LoRA saves grad+opt memory we already have at zero, and would only re-introduce the splice-collapse bug.** **(1) AC-off: OOM at seq6144 (job 8674108).** Cell B hit `torch.OutOfMemoryError` at **61.59 GiB allocated** (from the 21 GiB AC-on baseline, +300 MiB needed) — the per-layer hidden states to backprop through 64 layers at seq6144 are ~40 GiB, exceeding the idle headroom. Clean result: AC-off is activation-bound (revisit only via shorter-seq curriculum or AC-on+activation-offloading, a separate lever — the recipe requires AC=on for offloading, so it's not an AC-off fallback). This is also the empirical proof that LoRA wouldn't help (it died on activations). **(2) Bucketed-bs on flash: VALIDATED ~1.49× throughput (job 8674109).** The GEMM-occupancy lever packing was chasing, delivered on the proven flash path: the landed `LengthGroupedDistributedBatchSampler` with `pad_buckets=[4096,6144]` / `bucket_batch_sizes=[2,1]`, **plain causal per row (`mask=None` → flash native, NO flex, NO block-diag)**, 2-shape set (banned:1-safe). **bs2/seq4096 = fwd 6.32 + bwd 15.0 = 21.3s/mb for 2 samples = 10.66s/sample vs bs1/seq4096's 12.95s/sample = ~16.6% faster/sample — ~4× the pre-flash math bs2 result (4.5%, RUN4): flash's cheap attention is what makes batching amortize the FSDP gather.** Weighted over the corpus (79% ≤4096 → bs2, 21% → bs1) vs the `pad_to_fixed=6144` bs1 baseline: **~1.49× samples/sec** (two stacked sources: short samples run at 4096 not 6144 = less pad-GEMM, plus bs2 amortization). Clean 8/8, no banned:1, no OOM, loss monotonic 16.64→9.15. bs2 fits because 4096×bs2 attn transient == 6144×bs1 (memory-safe by construction); wider batching on the 6144 bucket would OOM like AC-off. **Updated working ladder: math 10% → flash ~20% → +compile ~22% → +bucketed-bs ~1.49× throughput. AC-off dead (activation-bound), packing dead (flex slow), LoRA irrelevant to the memory ceiling, multi-node orthogonal-linear.** **(3) SHIPPED to production default (soak job 8674383):** 15-step soak with flash+compile+bucketbs all on — NO recompile churn (compile `dynamic=True` caches the 2 bucket-shape graphs and reuses them), NO banned:1, loss monotonic, step1 188.75s (compile folded in) → steady 78.4s/it; **combined ~1.60× throughput** vs the flashON pad_to_fixed bs1 baseline (compile +6.8%/sample on top of bucketbs). Flipped `compile:{model:true}` + `pad_buckets:[4096,6144]` + `bucket_batch_sizes:[2,1]` into `sft_bioreason_qwen3_32B_stage1norm_xpu.yaml` (config parses, `test_async_loss_combo` passes). **(4) The ~30% MFU "ceiling" is a PROJECTION, not measured** (`19.7% ÷ 0.651 GEMM-time-share`); `with_flops` didn't populate on XPU so the GEMM kernels' real FLOP/s is unmeasured — a standalone GEMM microbench at Qwen3-32B layer shapes is needed to confirm whether ~30% is the XMX-at-bs1 ceiling or there's recoverable occupancy headroom (handed to a separate GEMM-focused effort). **Still TODO before a full production run:** longer multi-hundred-step soak + full-280 F_max parity (confirm no quality drift from bucketbs sample-ordering / compile numerics — both bf16-close in smoke, eval not yet run). Memory: `project_bioreason_sft_flash_native_levers_20260715`, `project_bioreason_sft_mfu_ceiling_20260715` (30% = projection). Launchers: `experiments/bioreason/pbs_1n_acoff_ab.sh`, `pbs_1n_bucketbs_ab.sh`, `pbs_1n_prod_soak.sh`.

- **2026-07-15 — BioReason 32B SFT throughput: flash ~2× HW-validated (10%→~20% MFU), compile +5.8% stacked, 2N HSDP tax SOLVED, packing built.** Four results advancing the 32B SFT MFU toward the ~30% dense-Aurora reference (AGPT-2B). **(1) XPU fused-flash re-profiled and CONFIRMED as the ~2× lever.** The 2026-07-09 profile that reported ~10% MFU ran the OLD math-SDPA path (`TORCHTUNE_USE_XPU_FLASH` unset); flash shipped after. Clean same-node flashOFF-vs-flashON A/B (job 8673644, seq4096, per-mb rank0 `perf_counter`): **flashOFF fwd 8.51 + bwd 16.9 → step 101.7s = 10.0% MFU**; **flashON fwd 3.87 + bwd 9.08 → step 51.9s = 19.7% MFU (−49%, 1.96×)**. Kineto trace (flashON): **SDPA 0.0%** (flash erased the pre-flash 10% softmax-S² tax), GEMM **65.1%** (now the floor → the packing target), elementwise 20.9%, collective 13.5% (up from 5% only because the step halved — a bigger *fraction* of a 2× shorter step, not slower), idle 4.5%. The earlier "flash ~= 16%" memory was a conflation: **16% was the COMPILE win, flash is a separate clean ~2×**. **(2) Per-layer backbone `torch.compile` stacks on flash.** seq6144 A/B on the real stage1norm production config (job 8673907, flash ON both cells, frozen-backbone): compileOFF step ~74.6s → compileON ~70.2s = **+5.8%** (→ 21.7% MFU), loss bf16-close (16.5248 vs 16.5251). Smaller than the seq4096 smoke's +10% because the elementwise fraction compile fuses shrinks as seq grows. One-time compile ~420s at step-1 (amortizes ~80 steps). **(3) The 2N HSDP scaling tax was `CCL_ATL_TRANSPORT=mpi`** — isolated A/B (job 8673012) proved the per-layer intra-node param all-gather is ~2.3× faster under `ofi`: 2N step ~35.8→20.0s, recovering the ENTIRE 1N→2N tax (2N-ofi ≈ 1N). The CLAUDE.md "mpi = 2× AllGather bandwidth" guidance is small-model-RL-specific and DISPROVEN for the 32B FSDP2 all-gather. Fix landed in `pbs_4n_sft_full.sh`/`pbs_2n_sft_smoke.sh` (`CCL_ATL_TRANSPORT=${…:-ofi} CCL_KVS_MODE=${…:-pmi}`; ofi REQUIRES pmi-KVS). **(4) Token packing — built, correctness-proven, but HW-REFUTED (jobs 8673929/8674030).** The intended next lever for the 65% GEMM floor: fill the fixed [1,6144] shape with real tokens from multiple docs (block-diagonal doc mask via compiled flex — flash is causal-only) instead of ~43% pad (mean real len 3498/6144). Fully built (`BioReasonPackedSFTDataset` + packed collate, `_sdpa_call` BlockMask→flex branch, `xpu_packed_block_causal_mask`, recipe `packing:` flag) and **correctness-proven on CPU** (packed logits bit-equal separate-doc forwards, zero cross-doc leak, multimodal splice works packed — `test_packing_equivalence.py` 4/4 incl a GO-packing regression, `test_packed_sft_dataset.py` 9/9). **The HW A/B killed it: packed-flex is ~4.9× SLOWER per step** (bwd 69-147s vs padded-flash 13.6s) **for only ~1.26× more real tokens = ~3.9× WORSE throughput.** The flex block-diag kernel that microbenched at 205ms/op (static warm mask) is 5-10× slower in the real loop, which rebuilds a fresh BlockMask every microbatch at seq6144 — **the de-risk microbench gave a false GREEN because a static/warm-input micro-loop doesn't represent a training loop that rebuilds the compiled-kernel input per step** (lesson banked). Also caught a real bug the CPU tests missed — the GO splice ignored `batch_idx_map` (crashed `GO token count 400 != 200` on 2-doc packs); fixed + regression-pinned. **Packing is SHELVED, default OFF, infra kept in-tree** for if a fast native block-diag FLASH backward ships (vendor SYCL-TLA). **Net: the working MFU ladder is math 10% → flash ~20% → +compile ~22%; packing does NOT join it, and multi-node stays the orthogonal linear lever.** Memory: `project_xpu_flash_bwd_reachable_dispatch`, `project_bioreason_sft_packing_scope_20260715` (REFUTED verdict), `project_bioreason_sft_hsdp_2n_forward_allgather_tax_20260714`, `project_bioreason_sft_throughput_diagnosis_20260708`. Launchers: `experiments/bioreason/pbs_1n_profile_flash_ab.sh`, `pbs_1n_pack_derisk_and_compile.sh`, `pbs_1n_packing_ab.sh`.

*For history prior to 2026-07-15 (older changelog entries) and all dated body
sections (stock-take, baselines, EP status series, framework findings, prioritized
next steps, etc.), see `docs/status_archive.md`. Current architecture/status
orientation lives in `CLAUDE.md`'s "Current Status" section.*
- **2026-08-09 — Keep-list live gate completed; production SFT queued.** Authorized Aurora job `8745794` loaded the Qwen3-32B checkpoint and trained projectors successfully; `check_run_health.sh` was **GREEN**, and generation health was **NO COLLAPSE** across 2 proteins × 4 variants. The 15-step smoke checkpoint emitted no true keep-list candidates on the live probe: candidate accuracy `0.2903`, precision `1.0000`, recall `0.0000`, gate `BELOW_60`. Treat this as an early-learning diagnostic, not a converged recipe result. Removed `set -u` from `experiments/bioreason/pbs_1n_gate_keep_list.sh` after Aurora Lmod's `ZSH_EVAL_CONTEXT` failure. Full 4-node keep-list SFT job `8745812` is now queued on `capacity` with `SAVE_EVERY=20`; no F_max claim is made until a trained checkpoint clears health and per-candidate gates.

- **2026-08-09 — Keep-list learning probe runtime diagnosis.** The authorized 2-node probe `8745839` reached Qwen3-32B model initialization but stalled before optimizer step 1 during compile/runtime setup; it was manually terminated after 10:47 wall minutes with no checkpoint. The resolved config confirms `backbone_builder: qwen3_32b` and `keep_list_target: true`; CCL reported narrow-affinity warnings when the probe was submitted with an explicit `ngpus` chunk. The loader’s generic `Loaded GEMMA4 base` message was corrected to report the actual configured builder in `recipes/dev/sft_bioreason_distributed_xpu.py`. Clean retry `8745853` uses the proven native 2-node request under `AuroraGPT`; it is queued awaiting the debug per-user slot. The 4-node production replacement `8745842` was rejected by a transient PBS `at_queue` placement shortage before execution; no model evidence was produced.
