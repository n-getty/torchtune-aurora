# Qwen3-32B TEXT-ONLY SFT — PROFILING control for the BioReason MFU diagnosis.
#
# Pure-text twin of sft_bioreason_qwen3_32B_profile_seq4096_xpu.yaml: SAME backbone (Qwen3-32B),
# SAME topology (12-tile full-shard, bs1, ga4, AC on), SAME LinearCrossEntropyLoss, SAME profiler
# block — but NO multimodal splice (stock full_finetune_distributed_xpu.py, alpaca packed).
# RUN A (bioreason) - RUN B (this) at [1,4096] isolates the multimodal-splice overhead.
#
# CAVEAT: alpaca `packed: true` yields [1,4096] packed sequences (good [1,4096] control), but the
# per-step token count is packed-dense (no right-padding) vs bioreason's padded 4096 — compare
# PER-TOKEN forward times, not raw. The splice delta (ESM3 cache loop + 2 projector GEMMs + scatter
# + clone) is large enough to read approximately.

name: sft_qwen3_32b_fitstest_profile_seq4096
output_dir: /lus/flare/projects/ModCon/ngetty/torchtune/experiments/bioreason/runs/${name}
base_model_path: /lus/flare/projects/ModCon/ngetty/models/Qwen3-32B

model:
  _component_: torchtune.models.qwen3.qwen3_32b

tokenizer:
  _component_: torchtune.models.qwen3.qwen3_tokenizer
  path: ${base_model_path}/vocab.json
  merges_file: ${base_model_path}/merges.txt
  max_seq_len: 4096

checkpointer:
  _component_: torchtune.training.FullModelHFCheckpointer
  checkpoint_dir: ${base_model_path}
  checkpoint_files: [
    model-00001-of-00017.safetensors, model-00002-of-00017.safetensors,
    model-00003-of-00017.safetensors, model-00004-of-00017.safetensors,
    model-00005-of-00017.safetensors, model-00006-of-00017.safetensors,
    model-00007-of-00017.safetensors, model-00008-of-00017.safetensors,
    model-00009-of-00017.safetensors, model-00010-of-00017.safetensors,
    model-00011-of-00017.safetensors, model-00012-of-00017.safetensors,
    model-00013-of-00017.safetensors, model-00014-of-00017.safetensors,
    model-00015-of-00017.safetensors, model-00016-of-00017.safetensors,
    model-00017-of-00017.safetensors,
  ]
  recipe_checkpoint: null
  output_dir: ${output_dir}
  model_type: QWEN2

resume_from_checkpoint: false

dataset:
  _component_: torchtune.datasets.alpaca_dataset
  source: tatsu-lab/alpaca
  train_on_input: true
  packed: true
seed: 1234
shuffle: true
dataloader_num_workers: 4

epochs: 1
max_steps_per_epoch: 6
batch_size: 1
gradient_accumulation_steps: 4   # match bioreason so profiler MICROBATCH schedule aligns
clip_grad_norm: 1.0

optimizer:
  _component_: torch.optim.AdamW
  lr: 1.0e-5
  weight_decay: 0.0
  fused: true

loss:
  _component_: torchtune.modules.loss.LinearCrossEntropyLoss

enable_activation_checkpointing: true
enable_activation_offloading: false
fsdp_cpu_offload: false
fsdp_reshard_after_forward: true
data_parallel_replicate_dim: 1
data_parallel_shard_dim: -1
tensor_parallel_dim: 1

device: xpu
dtype: bf16
compile: false

log_level: INFO
log_every_n_steps: 1
log_peak_memory_stats: true
metric_logger:
  _component_: torchtune.training.metric_logging.DiskLogger
  log_dir: ${output_dir}/logs

# IDENTICAL profiler block to the bioreason profile config.
profiler:
  _component_: torchtune.training.setup_torch_profiler
  enabled: true
  output_dir: ${output_dir}/profiling_outputs
  cpu: true
  cuda: false
  xpu: true
  profile_memory: false
  with_stack: false
  record_shapes: true
  with_flops: true
  wait_steps: 8
  warmup_steps: 4
  active_steps: 4
  num_cycles: 1
