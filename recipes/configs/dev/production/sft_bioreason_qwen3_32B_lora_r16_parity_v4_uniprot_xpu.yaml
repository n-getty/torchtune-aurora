# BioReason multimodal SFT — Aurora↔rbdgx3 PARITY leg A2v4: LoRA r16/a32/dropout0.05 on
# rbdgx3's real recipe (layer -1, UNBOUNDED projector), built on a converged Stage-1
# checkpoint trained WITH add_uniprot_summary=True (the confound-#5 fix, 2026-08-14).
#
# ★★ SUPERSEDES v3: v3 was correct on the layer-1/no-LayerNorm pairing and used a properly-
# converged Stage-1 base, but that base (and this LoRA leg) both trained WITHOUT
# add_uniprot_summary — a real, root-caused confound (rbdgx3 prompts/targets include a
# UniProt-summary line + instruction that Aurora's dataset_sft.py didn't support until this
# session). v3's Stage-1 was killed early (~1h54m in) once the fix landed, to avoid burning
# more compute validating the wrong objective. THIS config uses the add_uniprot_summary=True
# Stage-1 run (job 8754365) instead, so it is now genuinely apples-to-apples with rbdgx3 on
# BOTH axes that matter: the layer-1/projector pairing AND the prompt/target format.
#
# stage1_proj_dir below is a PLACEHOLDER until job 8754365 reaches a stable checkpoint —
# fill in the actual snapshot path (following the v2/v3 pattern: snapshot a stable step's
# protein_projection.pt/go_projection.pt to a dedicated dir BEFORE pointing this config at
# it, since job 8754365 is still live and overwrites its output_dir/epoch_0 every
# SAVE_EVERY steps — reading the live dir races exactly like the v2 collapse-gate incident).
#
# ⚠️ COLLAPSE GATE (still mandatory): train SAVE_EVERY=10, then run
# experiments/bioreason/probe_generation_health.py against the checkpoint BEFORE any eval.
#
# Recipe: recipes/dev/sft_bioreason_distributed_xpu.py

name: sft_bioreason_qwen3_32b_lora_r16_parity_v4_uniprot_xpu
output_dir: /lus/flare/projects/ModCon/ngetty/torchtune/experiments/bioreason/runs/${name}
base_model_path: /lus/flare/projects/ModCon/ngetty/models/Qwen3-32B
sft_data_dir: /lus/flare/projects/ModCon/ngetty/datasets/bioreason_sft_reasoning/data
esm3_cache_path: /lus/flare/projects/ModCon/ngetty/datasets/bioreason_sft_reasoning/esm3_cache_2048.safetensors
go_embedding_path: /lus/flare/projects/ModCon/ngetty/models/bioreason-pro-sft/go_embedding.pt

# Step-40 snapshot from job 8754365 (add_uniprot_summary=True Stage-1 run). Loss plateaued
# ~1.04-1.09 across steps 27-40 before this snapshot was taken.
stage1_proj_dir: /lus/flare/projects/ModCon/ngetty/torchtune/experiments/bioreason/runs/sft_qwen3_32b_stage1_layerneg1_uniprot_step40_snapshot

# ── Tokenizer (Qwen3) ─────────────────────────────────────────────────────────
tokenizer:
  _component_: torchtune.models.qwen3.qwen3_tokenizer
  path: ${base_model_path}/vocab.json
  merges_file: ${base_model_path}/merges.txt
  max_seq_len: 6144

# ── Model — LoRA r16/a32/dropout0.05, layer -1, NO projector LayerNorm ──────────
model:
  _component_: torchtune.dev.bioreason.model_native.BioReasonNativeModel
  backbone_builder: qwen3_32b
  hidden_size: 5120
  protein_token_id: 151643
  go_token_id: 151644
  enable_lora: true          # rbdgx3-recipe leg: LoRA-finetune the backbone
  freeze_backbone: false     # (ignored under LoRA; base frozen except adapters)
  freeze_projector: true     # LOCK the Stage-1-aligned (unbounded) projector.
  projector_output_norm: false  # rbdgx3's real recipe: NO LayerNorm.
  lora_rank: 16
  lora_alpha: 32
  lora_dropout: 0.05
  apply_lora_to_mlp: true
  esm3_cache_path: ${esm3_cache_path}
  go_embedding_path: ${go_embedding_path}

checkpointer:
  _component_: torchtune.training.FullModelHFCheckpointer
  checkpoint_dir: ${base_model_path}
  checkpoint_files: [
    model-00001-of-00017.safetensors, model-00002-of-00017.safetensors,
    model-00003-of-00017.safetensors, model-00004-of-00017.safetensors,
    model-00005-of-00017.safetensors, model-00006-of-00017.safetensors,
    model-00007-of-00017.safetensors, model-00008-of-00017.safetensors,
    model-00009-of-00017.safetensors, model-00010-of-00017.safetensors,
    model-00011-of-00017.safetensors, model-00012-of-00017.safetensors,
    model-00013-of-00017.safetensors, model-00014-of-00017.safetensors,
    model-00015-of-00017.safetensors, model-00016-of-00017.safetensors,
    model-00017-of-00017.safetensors,
  ]
  recipe_checkpoint: null
  output_dir: ${output_dir}
  model_type: QWEN2

resume_from_checkpoint: false
save_every_n_epochs: 1

# ── Dataset (identical distribution to Stage 1 + eval, INCLUDING add_uniprot_summary) ──
dataset:
  _component_: torchtune.dev.bioreason.dataset_sft.bioreason_sft_dataset
  data_files: ${sft_data_dir}
  max_seq_len: 6144
  max_protein_len: 2048
  num_go_tokens: 200
  protein_token_id: 151643
  go_token_id: 151644
  train_on_reasoning: true
  inject_go_pred: true
  add_uniprot_summary: true
seed: 1234
shuffle: true
collate_fn: torchtune.dev.bioreason.dataset_sft.bioreason_sft_collate_fn
dataloader_num_workers: 0

# ── Training envelope — conservative lr, mirrors Aurora's stage2 config ────────
epochs: 1
max_steps_per_epoch: null
batch_size: 1
gradient_accumulation_steps: 4
clip_grad_norm: 1.0

optimizer:
  _component_: torch.optim.AdamW
  lr: 2.0e-5
  weight_decay: 0.0
  fused: true

loss:
  _component_: torchtune.modules.loss.LinearCrossEntropyLoss

# ── Distributed / FSDP — full 12-way shard ──────────────────────────────────────
enable_activation_checkpointing: true
enable_activation_offloading: false
fsdp_cpu_offload: false
fsdp_reshard_after_forward: true
data_parallel_replicate_dim: 1
data_parallel_shard_dim: -1
tensor_parallel_dim: 1
pad_to_fixed: true

# ── XPU ───────────────────────────────────────────────────────────────────────
device: xpu
dtype: bf16
compile: false

# ── Logging ───────────────────────────────────────────────────────────────────
log_level: INFO
log_every_n_steps: 1
log_peak_memory_stats: true
metric_logger:
  _component_: torchtune.training.metric_logging.DiskLogger
  log_dir: ${output_dir}/logs

profiler:
  _component_: torchtune.training.setup_torch_profiler
  enabled: false
  output_dir: ${output_dir}/profiling_outputs
  cpu: true
  cuda: false
  profile_memory: false
  with_stack: false
  record_shapes: false
  with_flops: false
  wait_steps: 5
  warmup_steps: 3
  active_steps: 2
  num_cycles: 1
